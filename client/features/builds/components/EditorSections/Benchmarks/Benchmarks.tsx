import React from 'react';

import EditorSectionWrapper from '../EditorSectionWrapper';
const Benchmarks = () => {
  return (
    <EditorSectionWrapper section="Benchmarks / Videos">content checklist</EditorSectionWrapper>
  );
};

export default Benchmarks;
