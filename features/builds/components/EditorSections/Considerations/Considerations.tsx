import React from 'react';

import EditorSectionWrapper from '../EditorSectionWrapper';
const Considerations = () => {
  return <EditorSectionWrapper>Pros / cons</EditorSectionWrapper>;
};

export default Considerations;
